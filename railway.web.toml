# Отдельный сервис для веб-админки (опционально)
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python run.py web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
